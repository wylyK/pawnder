"use client";

import React from "react";
import CalendarPage from "@/components/calendar/CalendarPage"; // Adjust the path if needed

const Calendar: React.FC = () => {
  return <CalendarPage />;
};

export default Calendar;
